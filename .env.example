# Copy this to .env for local dev, or set these as Vercel Environment
# Variables (Project Settings → Environment Variables) for production.
# When both are set, the dashboard skips the manual paste-your-config
# screen and connects directly on load.

VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
